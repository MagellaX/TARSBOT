apiVersion: v1
kind: Service
metadata:
  name: {{ include "tars-ui.fullname" . }}
  labels:
    {{- include "tars-ui.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tars-ui.selectorLabels" . | nindent 4 }}